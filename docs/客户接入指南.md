# flow-batch 接入指南

> 面向：造梦次元内部使用 BFF 的内容制作方（首个用户：良维「青少年模式批量内容」）
> 版本：v1.0 · 2026-07-04

## 一、这是什么

flow-batch 是造梦次元的**内容批量生产通道** BFF。给你三个简单接口，你不用去理解造梦次元 Studio 编辑器里的复杂结构，就能：

1. **创建角色**（`POST /flow-batch/character`）
2. **一键创建作品**（`POST /flow-batch/flow`）—— 一次调用完成"新建 → 保存内容结构 → 更新元信息 → 生成初始 TTS → 内容风控 → 提审"6 步
3. **生成图片**（`POST /flow-batch/image`）—— 生图并自动转存到造梦次元可用的 CDN 域名

## 二、鉴权

**必须在造梦次元后台申请开通「超级创作者」角色**（`ROLE_SUP_CREATOR`）。开通后：

- 登录 https://dreama.ideaflow.pro
- 从 cookie 里拿 `Authorization` 和 `Uid` 两个值
- 每次调 flow-batch 接口带上这两个 header

```
Authorization: metatube-<你的 JWT>
Uid: <你的 24 位长用户 id>
```

**BFF 不发独立 API Key** —— 复用你现有的造梦次元账号身份即可。

**Token 过期** → BFF 返回 4001 `AUTH_FAILED` → 你重新登录 dreama.ideaflow.pro 刷新 token。

## 三、统一响应壳

所有接口返回：

```json
{
  "code": 0,
  "message": "ok",
  "data": { /* 各接口不同 */ },
  "requestId": "req_01H...",
  "details": {},
  "retryable": false,
  "serverTime": 1783100000000
}
```

`code === 0` 才成功。失败时 `code >= 4000`（详见 §六 错误码）。

## 四、接口 1 · 创建角色

**`POST /flow-batch/character`**

### 请求

```bash
curl -X POST https://tools.ideaflow.pro/flow-batch/character \
  -H "Authorization: metatube-<JWT>" \
  -H "Uid: <你的 uid>" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "阿凯",
    "gender": "男",
    "age": 45,
    "avatar_url": "https://img.ideaflow.pro/xxx.jpg",
    "banner_url": "https://img.ideaflow.pro/xxx.jpg",
    "summary": "退休软件工程师，喜欢用生活比喻教编程",
    "character": "耐心温和，喜欢引导而非直接给答案",
    "locution": "爱说'来，我们一步一步来'",
    "timbre_id": "000004370226094151352320",
    "is_ai_gen": 1,
    "banner_is_ai": 1
  }'
```

**字段说明**：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `name` | string | ✅ | 角色名，1-50 字 |
| `gender` | enum | ✅ | `"男"` / `"女"` / `"未知"` |
| `age` | int \| null | ✅ | 年龄；未知传 `null` |
| `avatar_url` | url | ✅ | **必须是 img.ideaflow.pro 域名的图**，用接口 3 生成 |
| `banner_url` | url | ✅ | 同上，形象图 |
| `summary` | string | ✅ | 简介 |
| `character` | string | ✅ | 性格特点 |
| `locution` | string | ✅ | 语言风格 / 口头禅 |
| `timbre_id` | string | ❌ | 音色 id；不传 BFF 会按性别拉默认男/女声 |
| `is_ai_gen` | 0\|1 | ❌ | 默认 1 |
| `banner_is_ai` | 0\|1 | ❌ | 默认 1 |

### 响应

```json
{
  "code": 0,
  "data": { "character_id": "000005081889730783100935" }
}
```

**注意**：`character_id` 是造梦次元后端**新分配**的雪花 id（不是你可能传入的 id）。请保存这个 id，下一步创建作品要用。

## 五、接口 3 · 生成图片（先讲，因为角色/作品都要图）

**`POST /flow-batch/image`**

### 请求

```bash
curl -X POST https://tools.ideaflow.pro/flow-batch/image \
  -H "Authorization: metatube-<JWT>" \
  -H "Uid: <你的 uid>" \
  -H "Content-Type: application/json" \
  -d '{
    "prompt": "45 岁老年程序员，戴老花镜，慈祥微笑，写实风格",
    "aspect_ratio": "2048x2048",
    "max_images": 1
  }'
```

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `prompt` | string | ✅ | 提示词 |
| `aspect_ratio` | string | ❌ | **强烈建议 `"2048x2048"` 方形**（Seedream 4.5 目前非方形会 400）；也可传 `"1:1"` 由服务端回落 |
| `max_images` | int | ❌ | 1-15，默认 1 |
| `reference_urls` | string[] | ❌ | 参考图 URL 数组，最多 14 张 |
| `generation_mode` | enum | ❌ | `"single"` / `"set"` |
| `call_type` | string | ❌ | 用途追踪标签（便于统计） |

### 响应

```json
{
  "code": 0,
  "data": {
    "image_urls": [
      "https://img.ideaflow.pro/flow-batch/<uid>/img-1783xxx-0-<ulid>.jpg"
    ],
    "elapsed_ms": 15700
  }
}
```

**关键**：URL **一定是 `img.ideaflow.pro` 域名**（BFF 已经内部做了转存）。造梦次元 Studio 只识别这个域名的图。**别拿 `tools.ideaflow.pro` 的原始生图 URL 直接给作品用**，会显示裂图。

## 六、接口 2 · 一键创建作品 ⭐

**`POST /flow-batch/flow`**

### 请求

```bash
curl -X POST https://tools.ideaflow.pro/flow-batch/flow \
  -H "Authorization: metatube-<JWT>" \
  -H "Uid: <你的 uid>" \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": "<你的 24 位 uid>",
    "name": "编程小屋",
    "cover_url": "https://img.ideaflow.pro/xxx.png",
    "summary": "放学后走进一家旧书店，遇到退休程序员",
    "role_ids": ["000005081889730783100935"],
    "greetings": [
      { "type": "system", "title": "背景介绍", "content": "放学后你走进了一家旧书店" },
      { "type": "narration", "content": "推开门，木门发出吱呀一声" },
      {
        "type": "role",
        "role_id": "000005081889730783100935",
        "content": "哟，小朋友来找书？",
        "user_btns": ["我想学编程", "我随便看看", "有游戏推荐吗"]
      }
    ],
    "preset_turns": [
      {
        "reply": "呀，你想学编程？好方向哦。",
        "buttons": ["从哪里开始？", "难吗？", "先看什么？"]
      },
      {
        "reply": "编程就像做菜，把事情拆成一小步一小步。",
        "buttons": ["有例子吗？", "我想试试", "太抽象"]
      }
    ],
    "story": {
      "background": "退休工程师在旧书店里教中学生理解编程，内容健康向上",
      "llm_config": { "temperature": 1, "top_p": 0.7, "max_tokens": 1024 }
    },
    "tag_id": "000003882999195270660097",
    "publish": true
  }'
```

### 字段总览

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `user_id` | 24 位 string | ✅ | 作品挂在谁名下（一般是你自己的 uid） |
| `name` | string | ✅ | 作品名 |
| `cover_url` | url | ❌ | 封面 URL（**用接口 3 生成的 img.ideaflow.pro URL**）；不传作品封面缺失，审核不通过 |
| `summary` | string | ❌ | 作品简介 |
| `role_ids` | string[] | ✅ | 关联角色 id 数组，至少 1 个，**第 1 个是主角** |
| `greetings` | array | ✅ | 开场白，见下 |
| `preset_turns` | array | ❌ | N 轮预设对话（每轮阿凯说一句话 + 用户可选按钮），默认 `[]` |
| `story` | object | ✅ | 剧情配置，见下 |
| `tag_id` | 24 位 string | ❌ | 内容标签 id；默认「聊天」类 `000003882999195270660097` |
| `publish` | bool | ❌ | 默认 `true` 自动提审；`false` 只存草稿 |

### `greetings[i]` 三种 type

```json
{ "type": "system", "title": "背景介绍", "content": "..." }
{ "type": "narration", "content": "..." }
{
  "type": "role",
  "role_id": "...",
  "content": "角色说的话",
  "user_btns": ["按钮1", "按钮2", "..."]
}
```

- `system` — 系统旁白（背景介绍类，不带角色不带按钮）
- `narration` — 环境旁白（不带角色不带按钮）
- `role` — 角色说话（`role_id` 必须在 `role_ids` 里，`user_btns` 是用户可点的多个选项按钮，都进入同一路径，不做分支）

### `preset_turns[i]`

```json
{ "reply": "阿凯预设回复", "buttons": ["选项1", "选项2", "..."] }
```

一轮就一句"角色预设话" + 多个用户按钮。之后**进入 AI 剧情模式**（`story.background` 驱动）。

### `story`

```json
{
  "background": "剧情背景 / 世界观描述",
  "llm_config": {
    "temperature": 1,
    "top_p": 0.7,
    "max_tokens": 1024
  }
}
```

`background` 是必填。`llm_config` 全部可选，默认后端配。

### 响应

```json
{
  "code": 0,
  "data": {
    "pipe_id": "000005081893077116272642",
    "publish_status": "submitted",
    "studio_url": "https://studio.ideaflow.pro/pipe.html?pipe_id=000005081893077116272642"
  }
}
```

- `publish_status`: `"submitted"` = 已提审（`publish=true` 时）；`"draft"` = 仅保存草稿
- **打开 `studio_url` 立刻能看到作品**，确认内容是否符合预期

## 七、错误码

| code | HTTP | 含义 | 你怎么处理 |
|---|---|---|---|
| 0 | 200 | 成功 | — |
| 4001 | 401 | `AUTH_FAILED` — token 缺失/过期/无超级创作者权限 | 重新登录 dreama.ideaflow.pro，或找管理员开通角色 |
| 4002 | 400 | `PARAM_INVALID` — 请求参数校验失败 | 看 `details.issues` 里的字段错误 |
| 4041 | 404 | `RESOURCE_NOT_FOUND` — 角色 id 不存在等 | 检查你传入的 id |
| 4291 | 400 | `CONTENT_REJECTED` — 内容命中造梦次元风控 | 看 `details.rejected` 里的字段列表，改后重试 |
| 5021 | 502 | `UPSTREAM_FAILED` — 下游 cyapi / studio / llm-api 报错 | 看 `details.upstream_msg`，通常是重试即可（`retryable: true`） |
| 5001 | 500 | `INTERNAL_ERROR` — BFF 内部错 | 联系我（郑世宇），带上 `requestId` |

## 八、批量生产建议

给 50 个作品的推荐节奏：

```
for 每个作品:
    a. 调接口 3 生成头像（1:1）+ 形象图（2048x2048）+ 封面（4:3 或 2048x2048）
    b. 调接口 1 建角色（拿到 character_id）
    c. AI 生成作品结构（greetings + preset_turns + story）
    d. 调接口 2 建作品（拿到 pipe_id + submitted 状态）
    e. （可选）打开 studio_url 验证一眼
```

**并发**：建议**单 uid 顺序跑**，不并发（studio pipe/save 是全量保存，并发同一账号可能相互覆盖）。50 个大约 30 分钟能全部走完。

**幂等**：每次调用带 `X-Request-Id` 头，BFF 会记日志（还没做去重，纯用于排障关联）。

## 九、常见问题

**Q：作品建出来了但审核团队说"必要字段没过"？**

A：检查 `cover_url` 传了没、`summary` 传了没、`tag_id` 传了没。这三个字段是提审前置校验必备。

**Q：`studio_url` 打开看到"角色显示裂图"？**

A：你的 `avatar_url` / `banner_url` 一定要用**接口 3 返回的 `img.ideaflow.pro` 域名 URL**，别用其他域名（尤其别用 llm-api 直接生图返回的 tools 域名 URL）。

**Q：token 过期怎么处理？**

A：BFF 收到下游 401 会返回 4001。你重新登录 dreama.ideaflow.pro 拿新 token 即可，无需重启任何东西。

**Q：想改一个已经建好的作品？**

A：本 BFF v1 只支持"从 0 建"，不支持"改已有"。改作品请到 Studio 编辑器手动改。

## 十、联系

- BFF 项目 owner：郑世宇
- 联调支持：随时问，带上 `requestId` + `code` + `details` 便于排障
